Deploying project to rinkeby network:

truffle migrates --network rinkeby

Reset the migrations in case of faliure:

truffle migrate --reset --network rinkeby

Check the smart contracs on etherscan:

truffle networks